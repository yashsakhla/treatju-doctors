import { CitiesDirective } from './cities.directive';

describe('CitiesDirective', () => {
  it('should create an instance', () => {
    const directive = new CitiesDirective();
    expect(directive).toBeTruthy();
  });
});
